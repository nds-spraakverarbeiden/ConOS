# Old Saxon Dependency Corpus, Release v0.1

The initial release of the data is automatically created by merging the subcorpora under `../src/`. 

To replicate corpus building, run:

	$> bash -e ./build.sh

Subsequent editions should be manual refinements of this data. So, the build scripts are provided for debugging purposes, mostly.